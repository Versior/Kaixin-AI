# TODO

本文档记录后续值得处理的事项。只放真实有价值的改进，不放泛泛而谈的口号。

## 稳定性

- 全站生图队列当前以内存状态为主，多实例部署前需要中心化队列。
- 3 分钟 3 张限流当前以内存状态为主，多实例部署前需要中心化限流。
- 统计口径需要继续统一：成功图片张数、失败任务数、限流次数应分开展示。
- 上游返回 `data: []` 时继续保留失败日志，并观察是否需要按渠道做健康度降级。

## 数据

- SQLite 适合单机，公网多人使用建议迁移到 PostgreSQL 或 MySQL。
- 增加后台数据备份和恢复入口。
- 画布项目如需跨设备同步，需要设计服务端项目表和媒体表。

## 产品

- 生图工作台可增加“限流剩余时间”提示。
- 用户排行可增加时间范围筛选，如今日、本周、全部。
- 生成历史可增加按模型、状态、时间筛选。
- 公告弹窗可支持 Markdown 预览。

## 安全

- 后台敏感操作增加二次确认。
- 管理员登录失败次数限制。
- 私有配置更新时避免把 API Key 打进日志。
- 图片代理和外部 URL 需要继续检查 SSRF 风险。
